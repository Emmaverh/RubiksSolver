data/cube_saved.json 

is het uitgangspunt

# use venv ........ (create better intructions_)

# create the req.txt file
Import the necessary modules
pip3 install pyqtgraph
pip3 freeze > req.txt

# OR
# use the req.txt file
pip3 install -r req.txt